// Package main embeds the generated Swagger documentation into the binary.
// The docs/ directory is populated by `make swagger` (swag init).
// Frontend assets are deferred to a separate proposal (migrar-nagiosql-frontend-vuejs).
package main
