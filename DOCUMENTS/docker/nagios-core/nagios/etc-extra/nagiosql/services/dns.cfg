define service {
    use                  local-service
    host_name            google-dns,cloudflare-dns
    service_description  DNS
    check_command        check_dns!nagios.org
    check_interval       10
    retry_interval       2
    max_check_attempts   3
    contact_groups       admins
}
