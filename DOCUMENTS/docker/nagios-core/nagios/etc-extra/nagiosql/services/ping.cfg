define service {
    use                  local-service
    host_name            linux-host,gateway,google-dns,cloudflare-dns
    service_description  PING
    check_command        check_ping!100.0,20%!500.0,60%
    check_interval       5
    retry_interval       1
    max_check_attempts   3
    contact_groups       admins
}
